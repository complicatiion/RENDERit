# Server

The included server is only a local static file server. It does not upload data, does not require internet access and does not provide authentication.

Default URL: `http://localhost:8888`

Default port: `8888`

Node override:

```powershell
$env:PORT=9000
node tools/local-server.js
```

### © complicatiion aka sksdesign · 2026
