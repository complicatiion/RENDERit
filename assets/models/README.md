# Model Assets

Place local `.obj` or ASCII `.stl` files here for manual import through the browser UI.

The app does not automatically scan this folder because browser security does not allow directory listing from a static runtime.

### © complicatiion aka sksdesign · 2026