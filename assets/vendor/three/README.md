# Three.js Runtime Slot

This folder is prepared for a future fully local Three.js branch.

This package currently uses a dependency-free WebGL2 renderer so that the app works offline without downloading packages. To migrate to Three.js, add official local Three.js files here and keep their MIT license in `assets/vendor/licenses/`.
