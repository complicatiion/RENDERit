# Start

Use `start_RENDERit.bat` on Windows. It starts a local server on port `8888` and opens the browser.

Fallback commands:

```powershell
python -m http.server 8888
```

or:

```powershell
node tools/local-server.js
```

### © complicatiion aka sksdesign · 2026